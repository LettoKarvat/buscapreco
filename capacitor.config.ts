import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.faives.scannerprodutos',
  appName: 'ScannerProdutos',
  webDir: 'dist'
};

export default config;
